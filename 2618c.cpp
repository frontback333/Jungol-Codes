#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    ll N;
    cin >> N;
}

int main() {
    cin.tie(0)->sync_with_stdio(0);

    ll T;
    cin >> T;
    while (T--) solve();

    return 0;
}